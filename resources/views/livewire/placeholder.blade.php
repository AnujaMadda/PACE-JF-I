<div>
    <h1 class="text-2xl font-semibold">{{ $title }}</h1>
    <div class="mt-8 rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center">
        <p class="font-medium">{{ __('This section is being built.') }}</p>
        <p class="mt-2 text-sm text-slate-600">{{ __('Planned for :phase.', ['phase' => $phase]) }}</p>
    </div>
</div>
